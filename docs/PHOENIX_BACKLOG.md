# Phoenix Backlog (Post-MESH-Migration Catalogue)

> **Status as of 2026-05-13: post-migration.** This catalogue reflects the **post-MESH-migration** ticket space (PHX-1000+). The 51 pre-migration tickets (PHX-0001–PHX-0074) have been categorised in [`phoenix-backlog/archive/MIGRATION_AUDIT.csv`](../phoenix-backlog/archive/MIGRATION_AUDIT.csv) as **carry-forward**, **obsolete**, or **absorbed into MESH doctrine**. The legacy catalogue is preserved at [`PHOENIX_BACKLOG_LEGACY.md`](PHOENIX_BACKLOG_LEGACY.md). The binding substrate doctrine is the MESH triplet ([`MESH_SUBSTRATE.md`](MESH_SUBSTRATE.md) + [`MESH_IMPLEMENTATION.md`](MESH_IMPLEMENTATION.md) + [`MESH_RETRIEVAL.md`](MESH_RETRIEVAL.md)); the operative implementation plan is [`MESH_MIGRATION_PLAN.md`](MESH_MIGRATION_PLAN.md).

The gap between PHX-0074 (last legacy ticket) and PHX-1000 is deliberate — it marks the doctrine boundary. Numbers are never reused. Each carry-forward ticket from the legacy backlog has a `migrated_from: PHX-XXXX` reference.

## How to file a new ticket

See [`phoenix-backlog/README.md`](../phoenix-backlog/README.md) for lifecycle rules. In short:

1. Pick the next free PHX-1000+ number.
2. Create an entry in this catalogue.
3. Create a YAML in `phoenix-backlog/` if the ticket is active (being worked, referenced from a PR, or emitted by a RunReport).

## Catalogue

| ID | Title | Status | Priority | Notes |
|---|---|---|---|---|
| PHX-1001 | MESH migration in progress | open | critical | Meta-ticket: the Strangler-fig plan. Tracks S1–S6. S1–S2 ✅; S2.5 seeded to 100k ✅ (full 4.81M blocked on a SeedConceptResolver RAM fix); S3 retrieval **mostly shipped** (PHX-1034: diversified injection MMR + weight-class, PPR/relation-conditioned Spreading Activation, frame-routing mechanism, Constellation, `mesh ask`, `MeshQueryRunReport`); **deferred within S3:** sub-mesh WL signature (`submesh.py`, optional) and the Kendall/Thyroxine frame-routing worked example (needs real frame vectors from Kadmos-v2 text ingestion — the structural seed carries zero frames). Query-path CSR latency surfaced (PHX-1041). Vision anchored (#168/#169). |
| PHX-1030 | Wikidata5m bulk seed (S2.5) | open | high | Interpolated bootstrap step between S2 and S3. Bulk-imports Q-ID-anchored Tier-1 nodes + edges from Wikidata5m; embeds first-paragraphs off-substrate. Spec: [`MESH_MIGRATION_PLAN.md`](MESH_MIGRATION_PLAN.md) §"Step S2.5". **Progress:** Smoke-1 (#162), Smoke-2 (#164), dedup engpass fixed (#165); operator-driven subnets built & bge-m3-embedded — 10k, 15k (`mesh-wiki-v1`), 100k (`mesh-wiki-100k`, ~984k edges). **Full 4.81M blocked** on a SeedConceptResolver RAM fix (100k OOM'd: resolver caches full node vectors; ~50–100 KB/node). |
| PHX-1031 | `mesh ingest --embedder` CLI flag | open | low | Operator-UX follow-up from PHX-1030 Smoke-1. Thread embedder selection through `theogony mesh ingest` so Kadmos vector-space alignment with the seed embedder is operator-explicit instead of env-hidden (`THEOGONY_EMBEDDING__MODEL_ID` / `THEOGONY_EMBEDDING__DIM`). Without alignment, `MeshTextVectorizer` silently falls back to deterministic hash-projection and Tier-2/Tier-3 linking is structurally broken. |
| PHX-1032 | Device-info logging in MESH embedders | open | low | Forensics follow-up from PHX-1030 Smoke-1. Add explicit device-selection logging (MPS / CUDA / CPU) to `BGEM3Embedder` (seed path) and `LocalSentenceTransformerEmbedder` (Kadmos path), and surface the chosen device in `MeshSeedRunReport` / `IngestRunReport` so silent-CPU-fallback is observable. Smoke-1 ran 0.48s/entity for bge-m3 — CPU-suspect but unprovable from current logs. |
| PHX-1033 | Wikidata5m bulk seed — Smoke-2 (10k entities, 50k triplets) | **resolved** | medium | **Resolved.** The predicted `_edge_keys` dedup engpass was fixed exactly as foreseen — a Lance-indexed `edge_dedup_index` (#165, mirroring `consolidated_qid_index`); 56k-edge backfill 0.73s, `load_dedup_keys()` 0.02s. Smoke-2 ran (#164) and scaling continued to 15k/100k. Retrieval-quality work continues under PHX-1034; full-scale seeding under PHX-1030. |
| PHX-1034 | S3 retrieval quality: production propagation operator | **resolved** | high | **Resolved — S3 `retrieval/` shipped.** The decided operators are now the substrate default (PPR relation-agnostic; relation-conditioned masked hop where a relation is available; raw/degnorm kept A/B-able). Full S3 path built on it: diversified injection (MMR + weight-class), frame routing, Constellation assembly, `theogony mesh ask`, `MeshQueryRunReport`. Validated end-to-end on `mesh-wiki-v1` (15k) and `mesh-wiki-100k` (101k/984k edges); SA primitive ~0.6s at ~1M edges. Query-latency anomaly (per-query CSR rebuild) filed as PHX-1041. YAML: [`PHX-1034.yaml`](../phoenix-backlog/PHX-1034.yaml). |
| PHX-1035 | MNLM: substrate as trainable weight matrix | open | high | **Tier-1 core (S5 heart), blocked on GPU.** Train the edge weights against the substrate's own retrieval primitive (Graph-GRPO + eligibility traces) so small-model + Chronik beats large-model-alone. Needs H100-class compute — blocked on hardware, not effort. Spec: [`etappes/mesh_native_lm_brief.md`](etappes/mesh_native_lm_brief.md) §5. |
| PHX-1037 | Substrate runtime perf: incremental CSR + weight quantization | open | medium | **Tier-2.** Replace per-tick full CSR rebuild with incremental delta-apply + compaction; FP16/INT8 edge weights. Unlocks write-throughput and memory at scale. Engineering, not research. |
| PHX-1038 | Sparse-attention-equivalence validation | open | low | **Tier-3.** Empirically show a Spreading-Activation step approximates an attention layer's retrieval on a controlled task — publishable validation of the "language model turned inside out" framing. |
| PHX-1039 | Variable-temperature / associative propagation mode | open | low | **Not central; later-maybe.** Query-conditioned propagation *temperature*: cool/targeted (PPR, deterministic-strongest) ↔ hot/associative (Boltzmann over top-K edges, far-point seeding, A×B dual-seed bridge discovery). The substrate already has the primitive (MESH_SUBSTRATE Stage-1 "activation temperature", today a therapy) — this would promote it to a retrieval mode and let the MNLM set it per query/step. Design template: the sibling **`../brainstorming`** toolkit (noise / pulse-texture / far-point / pairs / two-speeds; "creativity = novel + useful, push from the obvious"). Test its value on the emergent-judge (creativity), not link-prediction (precision). Sits above PHX-1034. |
| PHX-1041 | Query-path CSR caching (stop rebuilding per query) | open | high | **Tier-1, surfaced by S3.** `retrieve()` rebuilds the edge CSR every query via `load_all_edges()` (one JSON parse per edge). Measured on `mesh-wiki-100k`: csr 26,048 ms vs propagate 631 ms / ann 130 ms / assemble 661 ms — the SA primitive is sub-second at ~1M edges; the 26s is pure per-query edge deserialization. Fix: cache the CSR on the runtime (invalidate by Lance version / delta count) + build from Arrow columns, not JSON. Read-side companion to PHX-1037. YAML: [`PHX-1041.yaml`](../phoenix-backlog/PHX-1041.yaml). |
| PHX-1040 | Go-to-market & resource acquisition (bootstrapping) | open | medium | **Near-term, human-collaborative, negotiable.** Stage 0 of self-optimization ([`SELF_MODIFICATION.md`](SELF_MODIFICATION.md) §"scope"): acquire the *means to act* — funding, compute, energy, hardware, partnerships — since the physical stack starts in human hands. Includes advocacy/outreach, making the project legible to resource-holders, and the funding path. Not central long-term; the bridge from laptop to real-self-improvement resources. |
| PHX-1042 | Degree-hub bias in retrieval (hubs dominate every query) | open | medium | **Tier-1 retrieval quality, surfaced by live 100k testing.** A few very-high-in-degree nodes appear in the top-k of almost every query regardless of content. Measured on `mesh-wiki-100k`: `el panson` (Q2657718, in-deg 2,690) ranks #2 by activation in 5/5 unrelated queries despite not being an ANN seed; `united stated` (Q30, in-deg 25,275) dominates the `raw` operator entirely. `degnorm` demotes but does not remove the hub; the S3 diversified-injection layer (MMR + weight-class) fails to suppress it. Fix: degree-aware PPR damping and/or degree-aware MMR penalty. YAML: [`PHX-1042.yaml`](../phoenix-backlog/PHX-1042.yaml). |
| PHX-1043 | Verdict ceiling on anchorless seeds (Wikidata can never be `good`) | open | low | **UX/semantics question, not a bug.** Source-anchor nodes are created only by the document-ingestion path (`source_anchor.py`); the Wikidata5m seed has none, so `_verdict()` (mesh_explorer.py / mesh cli) can structurally never return `good` on a pure-Wikidata subnet — the ceiling is `partial` ("no source-anchored provenance reached"). Honest but misleading: it implies a reachable `good` that this dataset cannot produce. Decide: redefine `good` for connected+anchorless working sets, or surface the ceiling in the UI. Doctrine touch — escalate before changing verdict semantics. |
| PHX-1044 | Seed node names are `aliases[0]` (multilingual/noisy labels) | open | low | **Data-quality polish, surfaced in demo.** `_entity_name()` in `wikidata5m/importer.py` takes `record.aliases[0]` as the display name; wikidata5m aliases are multilingual and unordered, so the surfaced label is often non-English or a typo-redirect ("Berlim", "Germanz", "Albert Enstein", "Physysics"). Not a code bug, but it makes the Explorer look broken. Fix: prefer an English/canonical label when the dataset exposes one, else keep current behaviour. |
| PHX-1045 | Founding Demo — small, dense, fully Kadmos-read mythology mesh | open | high | **The demo the substrate deserves.** `mesh-wiki-100k` is a seeded skeleton (see PHX-1042/1043/1044); the mesh the doctrine specifies has only run at smoke scale. Build a founding mesh from Greek-mythology primary sources (Hesiod, Apollodorus, Ovid) read end-to-end by Kadmos v2; demo in three beats — animated activation with provenance, a genuine un-flattened contradiction (Aphrodite's parentage), a live Oneiros densification tick. Budget-capped at 100 EUR, operator-approved. Binding plan: [`docs/plans/FOUNDING_DEMO_PLAN.md`](plans/FOUNDING_DEMO_PLAN.md). YAML: [`PHX-1045.yaml`](../phoenix-backlog/PHX-1045.yaml). |
| PHX-1047 | ConceptResolver: ANN-backed identity resolution | open | high | **Write-side scalability, measured live.** Ingest fell from ~16 s to ~400 s/paragraph as the founding mesh grew to 157 consolidated nodes — the resolver checks candidates by scan. Fix: exact-match indexes + ANN neighborhood + top-k decision, O(log N). Write-side sibling of PHX-1041; root of the PHX-1030 RAM note. YAML: [`PHX-1047.yaml`](../phoenix-backlog/PHX-1047.yaml). |
| PHX-1048 | Kadmos ingest fleet — parallel readers, Batch API, model escalation | open | high | **Ingest reliability + economics, measured live.** Sequential single-model reading serialized behind rate limits (6.7 min/paragraph wall) and dropped 4/96 Theogony paragraphs on schema errors — including Aphrodite's sea-foam birth, recovered only by a manual Sonnet repair pass (0.06 EUR). Fleet design: parallel readers -> document-local mini-mesh -> one merge-commit; escalation instead of paragraph loss. YAML: [`PHX-1048.yaml`](../phoenix-backlog/PHX-1048.yaml). |
| PHX-1049 | Cap the structural chunk lattice (k-NN sparsification) | open | medium | **Edge-budget discipline, measured live.** shares_entities_with is 51% of all founding-mesh edges; the chunk layer forms a quasi-clique (92 nodes in the 64-127 in-degree bin) and grows O(C^2) per source. Cap structural neighbors at top-k, mediate the rest through concept nodes; must not regress the (currently perfect) connectivity. Doctrine-touch — flagged for MESH_SUBSTRATE review. YAML: [`PHX-1049.yaml`](../phoenix-backlog/PHX-1049.yaml). |
| PHX-1050 | Lance write amplification (one version per edge append) | open | high | **Storage discipline, measured live.** 15,340 Lance versions for 15,340 edges — a 400-node demo mesh occupies 29 GB (more than the 100k wiki seed). Fix: batch appends per paragraph/document + compact/cleanup as a tick op with operator-set retention. Sibling of PHX-1037. YAML: [`PHX-1050.yaml`](../phoenix-backlog/PHX-1050.yaml). |
| PHX-1051 | Identity attractor: generic hubs absorb entities at write time | in_progress | critical | **Identity integrity, forensically confirmed.** Description-cosine merges without lexical corroboration let the semantically generic work-node absorb Venus, Dione, and Zeus across four sources — 92 genealogy edges attach to the poem node, including daughter_of SELF-LOOPS. Write-side dual of PHX-1042. Fix: eager description-merge requires a shared tag or known label. YAML: [`PHX-1051.yaml`](../phoenix-backlog/PHX-1051.yaml). |
| PHX-1052 | Protagonist blindness in Kadmos v2 extraction | open | high | **Extraction fidelity, reproduced across models.** The passage's central figure lands as paragraph concept, never as entity — foam-birth yielded Cronos/Gaia/Cyprus/Eros but no Aphrodite; Iliad V no Venus/Dione. Cross-source identity has no anchor node. Fix at prompt/schema level; validation slices preserved. YAML: [`PHX-1052.yaml`](../phoenix-backlog/PHX-1052.yaml). |
| PHX-1053 | Merge-time identity evidence not persisted (Q-IDs evaporate) | in_progress | high | **Identity durability, measured live.** After the PHX-1052 fix, Aphrodite Q35500 merged correctly into the hymn concept — and the Q-ID died with the process: remember() updates only the in-memory registry. Fix: merge_identity_evidence() persists new Q-IDs to node + index; linker calls it in all three merge branches. End-to-end: Venus(Q35500) resolves to the same node afterwards. YAML: [`PHX-1053.yaml`](../phoenix-backlog/PHX-1053.yaml). |
| PHX-1054 | Uncorroborated LLM Q-IDs accepted as identity with score 1.0 | in_progress | high | **The strongest identity-corruption vector left after PHX-1051.** The Q-ID branch merged on any string match with score 1.0 and no corroboration, while the description (0.72) and tag (0.55) branches both carry the naming guard — and PHX-1053 then persists the merge, so one hallucinated identifier durably rewrites an entity. Fix: a plausibility guard dosed *weaker* than the naming guard, so Venus/Aphrodite-style bridges survive — naming **or** semantic corroboration, rejecting only when vectors disagree *and* nothing names. YAML: [`PHX-1054.yaml`](../phoenix-backlog/PHX-1054.yaml). |
| PHX-1055 | Kadmos ingestion yields nothing on providers that don't honour the JSON schema | in_progress | high | **A whole provider silently removed from the ingestion path.** Strict `model_validate_json` meant DeepSeek's `name`/`wikidata_id`/`subject`/`object` responses failed validation on *every* paragraph — 51 errors on one passage — so ingestion "succeeded" while producing only source anchors and zero concepts. Found by the live characterization layer added for PHX-1052; the mock tests could not see it, since the stub returns canonical field names by construction. Fix: `normalize_reading_payload` maps known aliases before validation, shared by the production and eval paths. YAML: [`PHX-1055.yaml`](../phoenix-backlog/PHX-1055.yaml). |
| PHX-1056 | Default seed count over-seeds retrieval; narrow seeding measurably wins | open | medium | **The seeding-ceiling result, applied to production defaults.** At `k_seeds=8` SA can largely only re-rank its own seeds (measured: retention 1.000 / rescue 0.000 at S=k on both multi-hop sets). Narrow seeding beat dense kNN held-out by +0.102 / +0.030 with no single-hop regression — and on the demo's own Beat-1 query it cuts the poem hub from 0.596 to 0.169 and surfaces the Hesiod source paragraph instead of Iliad noise, achieving what the PHX-1042 levers were built for without enabling them. Asks for an A/B across founding + wiki-100k before changing a default that affects Cockpit UX. YAML: [`PHX-1056.yaml`](../phoenix-backlog/PHX-1056.yaml). |
| PHX-1057 | Ingestion collapse located: per-candidate neighbour scans + per-edge writes | in_progress | high | **Supersedes PHX-1047's diagnosis — the ANN is fast (8.9 ms); the context scoring around it is not.** `_context_score` called `neighbor_ids()` per ANN candidate at **597 ms** each (two full-table scans, up to 24 per concept), and the reader wrote **each edge** as its own Lance transaction at **119 ms**. Fix: `adjacency_index()` builds every neighbour set from one columnar scan and the linker primes it once per run; the reader batches edge writes. Measured on the same passage + mesh: **109 s → 28.6 s**, `mentions_resolved` **37.9 s → 3.24 s**, `stored` ~21 s → 0.01 s — ingestion is now LLM-bound. YAML: [`PHX-1057.yaml`](../phoenix-backlog/PHX-1057.yaml). |
| PHX-1058 | A tick destroyed every parallel typed relation; saturation default 156× below doctrine | in_progress | critical | **The reported 69% edge loss was the merge key, not saturation.** `merge_edge_deltas` keyed by `(source, target)` alone, collapsing `mentions` / `co_mentions_in_paragraph` / `appears_in_source` between one pair into one edge — while the store's dedup index is keyed on source+target+**relation** (PHX-1033). Measured: founding **27,824 → 15,628, losing 2,520 distinct typed relations**; dormant until `mesh tick` shipped a driver. Second cause: `max_out_degree=64` against doctrine's tier-indexed 10K–1M (MESH_SUBSTRATE §3), truncating 116 nodes. Fixed both; ticks now land exactly on the distinct-triple count. YAML: [`PHX-1058.yaml`](../phoenix-backlog/PHX-1058.yaml). |
| PHX-1059 | The substrate never built any Lance index; every lookup was a full scan | done | high | **The last term in the ingestion collapse, and the largest.** `list_indices()` returned `[]` on every table, so each lookup scanned the whole mesh and grew with it — which is why resolution scaled with the *square* of the node count (measured: nodes ×2.07 → per-paragraph resolution ×4.26, with the LLM stage flat as control). Indexing the same 2,436-node mesh: `get_consolidated` 51.3→2.5 ms, label lookup 215.5→14.5 ms, ANN 59.7→8.0 ms. Built by the Oneiros tick, skipped below 512 rows. **Follow-up (#214): building once was only half of it** — Lance leaves rows appended after a build outside the index (64% of the mesh, measured), so `ensure_indices` now refreshes as well: stale vs. refreshed 88.4→3.3 ms on the label lookup. YAML: [`PHX-1059.yaml`](../phoenix-backlog/PHX-1059.yaml). |
| PHX-1060 | Node appends slow 30x as Lance versions pile up — one tiny transaction per node | done | high | **The next term after PHX-1059, and it is a write cost, not a read cost.** Profiling one paragraph into a 2,311-node mesh: `append_consolidated` is 78% of the resolution time at 83.9 ms/call. The driver is version count, not row count — 2,325 rows written in *one* transaction (13 versions) append in 2.6 ms, the same rows grown over 1,871 versions take 83.3 ms; pruning 1,893→13 versions restores 40.8→2.7 ms. Fourth instance of the one-transaction-per-item pattern (cf. PHX-1057 for edges). Route A (version pruning in the tick) shipped in #216: appends 73.4→2.5 ms, `stored`/paragraph flat instead of 10x. **Route B (buffering node writes) is closed** — after route A it was worth 1.7% of the resolution stage, against a PHX-1054 candidate-set risk. See YAML: [`PHX-1060.yaml`](../phoenix-backlog/PHX-1060.yaml). |
| PHX-1061 | The audit log wrote one Lance transaction per resolved concept — 84% of the resolution stage | done | high | **The term that closed the attribution gap.** `mentions_resolved` measured 186.5 s while every instrumented storage call inside it summed to 29.3 s. The rest was `MeshAuditLog.append` at **269.8 ms/call** — ten times what identity linking cost — one transaction per resolved concept. It hid because the same call measures **3.1 ms in isolation**; it is only expensive interleaved with other writes during a real ingest. Staged writes cut the slice from 26.5 s to 2.19 s. Fifth instance of one-transaction-per-item. YAML: [`PHX-1061.yaml`](../phoenix-backlog/PHX-1061.yaml). |
| PHX-1062 | The audit log was the one table no maintenance pass touched | done | medium | After the full read `mesh_audit` held 21,219 rows across **5,915 versions with no index**, while `prune_history` and `ensure_indices` covered every other table — and it is the most-written table there is. `list_recent(limit=10)` materialised the whole log and sorted it in Python to return ten rows: 625.6 ms, growing with every row ever written. Pruning takes the read to **2.1 ms** (127x); pushing the sort into the store is a further 27x on the pruned table. A `recorded_at` index was measured and **rejected** (2.1 → 1.9 ms, inside the noise). YAML: [`PHX-1062.yaml`](../phoenix-backlog/PHX-1062.yaml). |
| PHX-1063 | Q-IDs asserted by the reading model were stored as identity evidence; 127 of 130 are wrong | done | high | **The strongest identity-corruption vector, now measured.** Of 130 Q-IDs in the founding mesh, checked against Wikidata: 3 plausible, **122 name an unrelated entity, 5 do not exist**. Gaia carried the identifier of analytical chemistry, Cronus that of Anubis, Hera that of Willy Brandt. Asked three times, the model gives three different Q-IDs per figure, each at confidence 1.0. A stored Q-ID is durable and addressable, so a later reference merges straight into the wrong node via tier 4. Fix cuts between *looking up* and *writing down*: unverified Q-IDs still match against seeded identity (both wikidata5m hand-off tests pass unchanged) but are never persisted. Flags a tension with PHX-1053. YAML: [`PHX-1063.yaml`](../phoenix-backlog/PHX-1063.yaml). |
| PHX-1064 | The demo question fails on entity fragmentation, not on retrieval — consolidation is still a stub | superseded | high | **The full read closed the coverage gap and the answer did not improve.** Against a gold set of 18, the answer holds 1; sweeping `ann_limit`x`k_seeds` reaches 4 non-monotonically, `degree_beta` reaches 2. The ANN is the ceiling — the most informative passage sits at rank **409**. Cause: **eight separate nodes** describe Zeus as son of Cronos, one per mention. `stub_consolidation_phase()` raises NotImplementedError and the tick never calls it — all 6,816 nodes are tier 1, 5,275 still candidates. **Diagnosis corrected:** not S5 — PHX-1065. The substrate discarded entity names at write time; after fixing that and re-reading, nodes fell 6,816 → 5,002, merge candidates 1,823 → 8, and true duplicates at the same name are **zero**. The S5 merge pass has nothing to do here. YAML: [`PHX-1064.yaml`](../phoenix-backlog/PHX-1064.yaml). |
| PHX-1065 | The entity's name was discarded at write time; no node in the Theogony mesh is called Cronus | in_progress | critical | **The root under PHX-1064, and under most of the retrieval trouble.** Doctrine specifies a Tier-1 description as "name + key discriminators"; the write path stored only the discriminators and dropped the label entirely — **8 of 8 concepts lost their name**. So `find_consolidated_by_labels("Zeus"|"Cronos"|"Rhea")` returned **0 hits** on a passage about nothing else, and three distinct nymphs were stored as the same sentence. A merge detector on this substrate proposed 1,823 pairs and the top of the list was uniformly wrong. A/B: name hits 0/0/0 → 1/1/1, legible names 51 → 161. Does **not** reduce fragmentation — it makes identity representable so S5 dedup can be right. YAML: [`PHX-1065.yaml`](../phoenix-backlog/PHX-1065.yaml). |
| PHX-1066 | 93% of edges are asserted without judgement, under creation_contexts the doctrine does not list | done | high | Nodes get a graded identity decision; edges get none. Of 94,490 edges, **56.2% are `co_mentions_in_paragraph`** and only **6.9% carry a judged relation** — `father_of` 190, `mother_of` 183, `son_of` 180, against 53k proximity edges. Six of eight `creation_context` values in use are absent from MESH_SUBSTRATE §Edge. **But removing them changes nothing**: dropping 93% of the graph leaves gold recall at 3/14 and moves two ranks. A doctrine-and-correctness argument, not a quality one. **Decided:** MESH_SUBSTRATE §Edge now names two classes — *asserted relation* (something claimed a relation holds) and *observed adjacency* (two things occurred together, no claim). The distinction was already there unnamed: `hebbian_co_fire` sat among the contexts with the note that such an edge has "no semantic story to tell". Recording co-occurrence is neither ungrounded nor silent; presenting it *as* an assertion would be, and naming the class is what keeps that line visible. YAML: [`PHX-1066.yaml`](../phoenix-backlog/PHX-1066.yaml). |
| PHX-1067 | No instrument existed for retrieval quality on the founding corpus; three diagnoses were wrong because of it | in_progress | critical | **Phase 0: build the measurement before the fixes.** Every retrieval claim so far rested on a gold set invented at measurement time from one question — and three diagnoses drawn from it were wrong. Ships 32 questions over the founding corpus, each carrying its answering passage, scored against the live substrate. Answers are entity **names** (possible only since PHX-1065), and **coverage is reported apart from recall** because blending them caused two of the three errors. First baseline: **coverage 100%, recall 48%**, 13 of 32 fully answered. The corpus is fully read; the entire shortfall is retrieval. YAML: [`PHX-1067.yaml`](../phoenix-backlog/PHX-1067.yaml). |
| PHX-1068 | Retrieval never looked up the entities a question names; recall 48% → 65% | in_progress | high | Seeding was pure vector search — but **the answer to a question does not resemble the question**. Eunomia, Dike and Eirene rank 2345/2578/2764 for "What children did Themis bear to Zeus?". Measured: missed entities sit at median ANN rank **778 of 3,000**, yet **62 of 62 expected entities are one hop from an entity the question names** (chance baseline 6.6%). Seeding on those anchors through the index the linker already uses: **48% → 65%**, full answers 13/32 → 18/32, for **8 ms**. Also makes PHX-1066 matter: with anchors, dropping co-occurrence edges gives a further 57% → 64%. YAML: [`PHX-1068.yaml`](../phoenix-backlog/PHX-1068.yaml). |
| PHX-1069 | Recall is 65% at top_k=30 and 95% at 200 — the ranking is right, the budget is tight | done | medium | A point estimate reads as "retrieval cannot find the answers". The curve says otherwise: **40/53/65/74/85/95%** at top_k 10/20/30/50/100/200 — 95% of expected entities sit in the top **4%** of a 5,002-node substrate. "Cannot find" and "cannot fit" need different work. Cost is not the constraint, so the default moved **30 → 50**: +9 points for **3.2 ms**. Not further — 100 buys eleven more for 15 ms, but whether a wider constellation completes an LLM's answer or dilutes it is not a question recall can settle. Two hypotheses refuted before shipping: removing co-occurrence edges (**−10 points on narrative** questions, hidden by a 66% genealogical gold set) and excluding question anchors (**25% of expected answers are anchors**). YAML: [`PHX-1069.yaml`](../phoenix-backlog/PHX-1069.yaml). |
| PHX-1070 | Relation-type-aware propagation — measured, and simpler than proposed | measured_and_shipped_as_a_lever | high | The proposed mechanism (embed relation descriptors, let the query select which conduct) was not needed: a plain multiplier on edges whose relation resolves to a Wikidata property gets most of it. The old ceiling (58 → 66%) is superseded — it predates name anchors and top_k=50, and today's baseline of **74%** sits above it. Selecting typed edges is 89% genealogical but **−14 narrative** (the typed subgraph reaches 121 of 163 gold entities); weighting them keeps every node reachable — at boost 3, **three questions improve and none regress**, 33 → 36 of 47 answered in full. Controls: all-asserted 78%, random-same-size 76%, typed **82%**, so the typing carries signal beyond mere assertedness. Ships **default off** — one corpus, one gold set, and it moves exactly one question kind (narrative recall is flat at 86% for every boost). Found [PHX-1078] on the way: the first lever did nothing because the index reported 7 typed edges where the store held 1,420. YAML: [`PHX-1070.yaml`](../phoenix-backlog/PHX-1070.yaml). |
| PHX-1071 | Aliases discovered at merge time are never persisted; referring expressions stay separate nodes | measured_and_rejected | high | The mesh holds **six separate Zeus nodes** — "son of Cronos", "her father", "Son of Chronos" — and **every one carries `Zeus` in its own tags**. The linker's tag tier did not fire, and `merge_identity_evidence` persists **only Q-IDs** despite its name: the linker hands aliases to an in-memory registry that dies with the run. **Corrects my own same-day claim** that there were zero duplicates — that was keyed on primary names. Keyed on shared tags there are 250 groups, but most are *category* tags ("epic poem" spans the Little Iliad and the Aethiopis) and must never be merged; the discriminator between alias and category is **now measured, and it fails**: across 251 groups it puts `year` (1763, 1333) and `city` (Ilium, cities) on the alias side and `apollo` (Far-Shooter, Far-Worker) on the category side. And the obvious fallback has no ground either — **Apollo has 0 nodes named for him and 8 tagged with him**, Athena 0 and 6, because the extractor uses the text's own wording and Hesiod rarely says plainly "Apollo". Coreference in a text that circumscribes its figures is not a string problem. **Built, measured, rejected (#229 closed):** persisting aliases made recall **worse at every budget** (58% vs 65% at top_k=30) and did not reduce node count (5,007 vs 5,002). Aliases are extra label-index entries, and PHX-1068's name-anchored seeding reads that index — more entries per name means blunter anchors. Fewer fragments ≠ better findability. What the three-way comparison *does* show: naming nodes alone is coverage 59% → 100% and recall 29% → 65%. YAML: [`PHX-1071.yaml`](../phoenix-backlog/PHX-1071.yaml). |
| PHX-1072 | Relation descriptors are free text; `Edge.pids` was empty on every edge | in_progress | high | **2,672 distinct descriptors for 6,489 judged edges**, 1,913 of them singletons, `father_of` (190) beside `father of` (58). Nothing downstream can group by that. A curated, API-verified table maps 28 descriptors onto 10 Wikidata properties — **19%** of judged edges. Two refusals carry it: the model is never asked (it confabulated 127 of 130 Q-IDs), and **only direction-faithful mappings exist** — `killed` maps to nothing because P157 reads "killed by", and a table with an inversion flag works until someone forgets it and the genealogy is backwards. P8810 ("parent") beside P40 ("child") is what makes that affordable. YAML: [`PHX-1072.yaml`](../phoenix-backlog/PHX-1072.yaml). |
| PHX-1073 | The gold set was 66% genealogical, and that bias nearly waved through a regression | in_progress | medium | Removing co-occurrence edges read as **+2 on the aggregate** and was +4 genealogical against **−10 narrative** — with two thirds of the questions on one side, the aggregate hid it. 15 questions added from the works the set barely touched (Homeric Hymns, Shield of Heracles): 32 → 47 questions, 83 → 111 expected entities. (The genealogical share was hand-counted at **51%** here; the labelling is checked in since PHX-1080 and gives **55%** by question, 68% by entity — three separate hand-counts of this set gave three answers, which is the reason it is now a field.) Recall on the wider set **68%**, so the widening did not flatter the system. Two gold-rot corrections recorded: an expected answer fixed against the *corpus* rather than the mesh (circularity), and an evidence quote of sixteen characters that turned out not to occur in the text at all — caught by the minimum-length test. YAML: [`PHX-1073.yaml`](../phoenix-backlog/PHX-1073.yaml). |
| PHX-1074 | The tick deep-copied every edge to merge nothing; and it costs recall on an unreinforced mesh | in_progress | medium | Profiling refuted a lazy-decay proposal **before it was built**: decay is **0.4%** of the pass (0.02 s of 4.83 s). The real cost was `merge_edge_deltas` spending **1.63 s to merge zero deltas** — a deep copy of every edge. Shallow copy: tick **4.90 → 3.80 s (−22%)**, substrate and retrieval unchanged. Second finding, a property not a defect: **each tick costs ~0.75 recall points** (65% → 62% over four ticks) because decay runs while nothing reinforces — the doctrine's "unless reinforced" clause is load-bearing. A recall figure is only comparable at equal tick count. YAML: [`PHX-1074.yaml`](../phoenix-backlog/PHX-1074.yaml). |
| PHX-1075 | Proposal: 56% of the edge table is derivable and need not be stored | measured_viable | medium | **Measured exactly:** the co-occurrence edges are the transitive closure of `mentions` within each paragraph, with no remainder — 53,122 derivable, 53,122 stored, **0 stored but not derivable**. 56% of the table implied by 7,229 edges, a 7.3× compression of the dominant class and the same cut to every O(E) tick pass. It would also make PHX-1066's asserted/observed distinction physical rather than conceptual. **Both objections measured and fallen:** retrieval is *identical* (65% stored vs 65% derived with reconstructed weights, exact set equality; a flat weight costs 5 points), and there is no per-edge history to lose — the 53,122 edges carry **13 distinct weights** tracking `born_at`. Tick **3.71 → 1.86 s**, CSR build 171 → ~200 ms. What remains is engineering, not a question. YAML: [`PHX-1075.yaml`](../phoenix-backlog/PHX-1075.yaml). |
| PHX-1076 | Unbounded dependency ranges took CI red four times in one week | open | high | Typer metavar (#191), `LanceQueryBuilder.metric` (#213), sentence-transformers shipping type info (#218), and now **anthropic 1.0.0 swapping httpx for httpx2**. The last is mutually exclusive — 0.96 rejects an `httpx2.AsyncClient`, 1.0 rejects an `httpx.AsyncClient` — and `respx` mocks httpx, so a version-adaptive helper was written and discarded as insufficient. Pinned `<1.0` to unblock. **Every one was invisible locally**, two broke files nobody had edited, and one was a *good* upstream change. Options for the owner: CI lockfile, upper bounds, or a drift-watch job. YAML: [`PHX-1076.yaml`](../phoenix-backlog/PHX-1076.yaml). |
| PHX-1077 | Decay and reinforcement are three orders of magnitude apart; the loop balances at ~179,000 queries per tick | open | high | The doctrine says concepts evaporate "**unless reinforced**" — measured, reinforcement cannot reach. On disjoint halves of the gold set: baseline 80%, tick-only 78%, **reinforce+tick 78%** with 1,472 deltas written. The weight sums say why: a tick costs **−777.5**, reinforcement adds **+0.1**. Balance would need ~11.4M deltas — **179,000 queries per tick** — or a learning rate of 77.8 instead of 0.01. Neither mechanism is broken; nothing calibrates them against each other. Proposal: **tick on query volume, not on a clock**. YAML: [`PHX-1077.yaml`](../phoenix-backlog/PHX-1077.yaml). |
| PHX-1078 | Every Constellation showed the co-mention and hid the claim | done | high | `descriptor_index` holds one descriptor per node pair; the store deliberately keeps several (dedup key is `(source, target, descriptor)`). Scan order chose. Of 1,323 pairs joined by a relation resolving to a Wikidata property, **1,316 displayed something else** — `Cronos -> Zeus` read `co_mentions_in_paragraph` while `father_of` sat behind it. 5,540 pairs corrected in all. Fixed with `descriptor_rank`: named relation > assertion > bookkeeping, which is MESH_SUBSTRATE §"Asserted relations and observed adjacency" applied at read time. Recall unchanged at 74%, as it must be — descriptors do not propagate. Invisible to every row-level check: each displayed descriptor named an edge that really existed. Surfaced sideways, from a typed-edge lever that did nothing because the index reported 7 typed edges where the store held 1,420. YAML: [`PHX-1078.yaml`](../phoenix-backlog/PHX-1078.yaml). |
| PHX-1080 | The genealogical/narrative split was never checked in, so no number using it reproduced | done | high | Every claim about this substrate is reported split by question kind, because PHX-1066 showed the aggregate hides a **−10 narrative** regression under **+2 overall**. The split itself lived as a regex written fresh at each measurement. Three hand-labellings of the same 47 questions gave **51% / 55% / 57%** genealogical. Re-measured proof: the `typed_edge_boost` table read 64%/81% under the lost regex and **68%/86%** under checked-in labels — while the aggregate row is identical either way, which is why the drift went unnoticed. Fixed by making `kind` a field with its rule and its five borderline calls written beside it. Share: **55% by question, 68% by entity**. Corrected everywhere the retired 32-question set's 66% was still quoted, including a README that advertised **32 questions** six days after the set became 47 — the test asserted `len(gold) >= 30`, and a floor cannot notice that a published number stopped matching the file. Bonus: narrative recall turns out **flat at 86%** for every boost, so PHX-1070's lever costs those questions nothing. YAML: [`PHX-1080.yaml`](../phoenix-backlog/PHX-1080.yaml). |
| PHX-1081 | A category word in the question evicted the subject from the anchors | done | high | Anchor spans were read longest-n-gram-first and the first one took the whole budget. `greek mythology` matches 18 nodes, so **"In Greek mythology, who is the father of Zeus?" never looked `Zeus` up at all** — and two differently-worded questions returned the **identical eight anchors**. Fixed by ordering spans by fanout, narrowest first: a label matching one node is a name, one matching eighteen is a category. Two alternatives rejected by measurement — a fanout cut-off would sit in a four-node gap (widest real name `Hermes` at 11, narrowest category word at 15), and a per-span quota fixed the broad case by taking the plain question from six anchors to three. Cost 90.4 → 96.4 ms. **0 of 47 gold questions hit this path**, so the property is pinned by test rather than by widening the set — and the first fixture draft passed against the defect until the insertion order was fixed. YAML: [`PHX-1081.yaml`](../phoenix-backlog/PHX-1081.yaml). |
| PHX-1082 | A failing tick emptied all three edge tables, and reported nothing at all | done | high | `replace_all_edges` was three `delete("true")` calls then three `add()` calls, with a docstring calling it "atomically replace". Six transactions; an exception in the second half left **all three edge tables at zero rows**, reproduced by making the first `add` raise. Correction to the first read: the edges *were* recoverable — `prune_history` runs after the rewrite, so the prior Lance snapshot survived — but nothing in the codebase calls `restore`. The drained Hebbian deltas had no snapshot at all, because `drain()` unlinks the sidecar first. Fixed with `mode="overwrite"` (atomic *per table*, stated rather than implied), delta restore on failure, and a real error path in the CLI — `verdict="good"` was a literal on a branch that could not fail, so a dead tick left no report. Also: `prune_history` silently omitted `edge_dedup_index`, which had kept **237 versions** against 1 for its siblings. Verified on a byte copy: 94,490 edges in and out in 4.2 s. YAML: [`PHX-1082.yaml`](../phoenix-backlog/PHX-1082.yaml). |

### Carry-forward tickets from the legacy backlog

Each has `migrated_from: PHX-XXXX`.

| new ID | migrated_from | Title | Status (initial) |
|---|---|---|---|
| PHX-1002 | PHX-0013 | Chronese Canonical Semantic Layer | open |
| PHX-1003 | PHX-0014 | Metis Advisory Runtime | open |
| PHX-1004 | PHX-0015 | Fast/Slow Cognition and Opposition Protocol | open |
| PHX-1005 | PHX-0017 | Sensorium: Multimodal Acquisition Adapters | open |
| PHX-1006 | PHX-0018 | Chronik-to-Model Distillation and Hardware Co-Design | open |
| PHX-1007 | PHX-0019 | Hestia: Human Flourishing Guardian | open |
| PHX-1008 | PHX-0033 | Pre-curated Wikidata subset for travel literature | **obsolete** — superseded by the Wikidata5m bulk seed (PHX-1030) + Kadmos v2 for arbitrary text; the travel-lit subset was a Gen-1 demo artifact |
| PHX-1009 | PHX-0034 | Entity-resolution quality benchmark | open |
| PHX-1010 | PHX-0036 | Re-evaluate Gemini 3.1 Flash Lite once GA | **obsolete** — time-passed (model question stale by 2026-06); re-file against a current model only if an embedder/LLM re-eval is actually scheduled |
| PHX-1011 | PHX-0037 | LLMRateLimitError as first-class exception | open |
| PHX-1012 | PHX-0038 | AuditingLLMProvider wrapper | open |
| PHX-1013 | PHX-0039 | Token-bucket backoff inside LLMProvider | open |
| PHX-1014 | PHX-0040 | Per-stage RPM throttling in IngestionPipeline | open |
| PHX-1015 | PHX-0045 | pytest-xdist parallel test execution | open |
| PHX-1016 | PHX-0049 | AnswerSynthesizer system prompt packaging | open |
| PHX-1017 | PHX-0055 | CI smoke-test against live default LLM | open |
| PHX-1018 | PHX-0058 | Aggregated stub detection (curiosity) | open |
| PHX-1019 | PHX-0060 | Domain Clusters / Cognitive Centers | open — **home of the expert-MNLM fragmentation idea (Tier-2):** cluster the mesh into topic regions; each region + a specialised MNLM = an expert. **Locality greenlight measured (PHX-1034, 100k):** PPR keeps activation in ~78 effective nodes / 100,883 (participation ratio 77 vs 338 raw) → sharding is viable. |
| PHX-1020 | PHX-0061 | Vector-Routed Federation | open — pairs with PHX-1019: cross-expert routing / partition-pruning = the "activation planner" (Postgres-style cost-based seed/shard selection). |
| PHX-1021 | PHX-0064 | Portable Constellation | open |
| PHX-1022 | PHX-0066 | Hosted Pantheon MCP Service | open |
| PHX-1023 | PHX-0067 | Eris — Adversarial Defender | open |
| PHX-1024 | PHX-0068 | Nemesis — Hybris Auditor | open |
| PHX-1025 | PHX-0069 | Hosted MCP SSE session affinity | open |
| PHX-1026 | PHX-0070 | StubLLM synthesis raises before completion | open |
| PHX-1027 | PHX-0071 | Mnemosyne — self-reflective backlog auditor | open |
| PHX-1028 | PHX-0072 | Proteus — twin-agent A/B testing | open |
| PHX-1029 | PHX-0074 | Iris — the Pantheon Cockpit | open |

### Tickets absorbed into MESH doctrine

These are **not** re-filed. Their concerns are now part of the MESH triplet. The working tickets listed here link to the MESH-triplet section that subsumes them for traceability:

| Legacy ID | Title | Absorbed by |
|---|---|---|
| PHX-0001 | Custom Knowledge Store Engine | MESH_IMPLEMENTATION.md, MESH_SUBSTRATE.md |
| PHX-0002 | Hierarchical + Heterogeneous Embedding Spaces | MESH_SUBSTRATE.md §Node anatomy |
| PHX-0016 | Non-Chronological Knowledge Topologies | MESH_SUBSTRATE.md §"No hierarchical pointer field" |
| PHX-0020 | Operative Knowledge: The Fifth Form | MESH_RETRIEVAL.md diversified injection + constellation assembly |
| PHX-0048 | KnowledgeStore.batch_update_scores | MESH_IMPLEMENTATION.md §delta buffer + CSR rebuild |
| PHX-0052 | HNSW + filter pushdown for vector_search | MESH_IMPLEMENTATION.md §"Nodes — LanceDB" |
| PHX-0056 | Activation Engine v1 | MESH_RETRIEVAL.md §"Spreading Activation as batched SpMV" |
| PHX-0057 | Edge-Pheromone trails + Slow-Path emancipation | MESH_SUBSTRATE.md §"Hebbian update" + eligibility traces + super-linear decay |
| PHX-0059 | Morpheus-as-Associator + multi-layer connectivity | MESH_IMPLEMENTATION.md §"Oneiros — implementation order" |
| PHX-0062 | Negative Knowledge / Anti-Bullshit Layer | MESH_SUBSTRATE.md §"Agent-driven cleanup" + §"Pathology and therapy" |
| PHX-0063 | Chronik-Diff | MESH_IMPLEMENTATION.md §"Audit ledger" + Lance version snapshots |
| PHX-0065 | Pantheon as Time Machine | MESH_IMPLEMENTATION.md §"Lance MVCC versioning" + checkout |
| PHX-0073 | Asklepios — Healer agent | MESH_SUBSTRATE.md §"Agent-driven cleanup" + §"Therapy" |

### Tickets declared obsolete

These are **not** re-filed. They address Gen-1 features (mostly Neo4j-specific) that the MESH substrate replaces:

| Legacy ID | Title | Reason |
|---|---|---|
| PHX-0041 | Detective default-on threshold re-evaluation | MESH has different anomaly detection (Argus pathology in S5) |
| PHX-0042 | Cypher query-plan audit for Neo4jKnowledgeStore | Neo4j-specific; MESH forbids traditional graph databases |
| PHX-0043 | Async-pipeline IngestionPipeline writes against Neo4jKnowledgeStore | Neo4j-specific |
| PHX-0044 | Read-replica readiness audit of Neo4jKnowledgeStore | Neo4j-specific |
| PHX-0046 | batch_upsert_nodes/batch_upsert_edges via UNWIND+MERGE | Neo4j-specific |
| PHX-0047 | Neo4jSettings password default | Neo4j-specific |
| PHX-0050 | ConstellationAssembler single bulk-edges Cypher | Neo4j-specific; MESH CSR provides adjacency via SpMV |
| PHX-0051 | MultiHopBreakdown.nodes_per_hop schema | Gen-1 query-result shape |
| PHX-0053 | traverse Cypher: strip embeddings | Neo4j-specific |
| PHX-0054 | Re-measure PHX-0046 batch_upsert speedup | Neo4j-specific |
